<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title>Verification Successful</title>
	<style>
		body { font-family: Arial, Helvetica, sans-serif; background:#f5f9f4; margin:0; padding:0; }
		.container { max-width:600px; margin:30px auto; background:#ffffff; border-radius:14px; overflow:hidden; box-shadow:0 4px 14px rgba(0,0,0,0.08); }
		.header { background:#1b5e20; padding:26px 26px 18px; color:#fff; }
		.logo { font-size:22px; font-weight:600; letter-spacing:.5px; }
		.hero { padding:34px 34px 16px; text-align:left; }
		h1 { font-size:24px; margin:0 0 14px; color:#1b5e20; }
		p { line-height:1.55; color:#333; margin:0 0 16px; }
		.status { background:#e8f5e9; border:1px solid #b6dfbb; padding:18px 22px; border-radius:10px; font-size:15px; font-weight:500; color:#1b5e20; margin:24px 0 6px; }
		.cta { display:inline-block; margin:26px 4px 10px; background:#1b5e20; color:#fff !important; text-decoration:none; padding:14px 26px; border-radius:8px; font-weight:600; font-size:15px; }
		.footer { background:#fafafa; padding:20px 26px; text-align:center; font-size:12px; color:#777; }
		a { color:#1b5e20; }
		.next-steps { background:#fcfcfc; border:1px solid #eee; padding:16px 18px; border-radius:10px; margin-top:26px; }
		ul { padding-left:18px; margin:8px 0 16px; }
		li { margin:6px 0; }
		@media (max-width:620px){ .hero { padding:28px 22px 10px; } h1 { font-size:21px; } }
	</style>
</head>
<body>
	<div class="container">
		<div class="header">
			<div class="logo">YourApp</div>
		</div>
		<div class="hero">
			<h1>Email Verified 🎉</h1>
			<p>Hi <strong><%= name %></strong>, your email address has been successfully verified. You're all set to start using your account.</p>
			<div class="status">Your account is now active and secure.</div>
			<div class="next-steps">
				<p style="margin-top:0; font-weight:600;">What you can do next:</p>
				<ul>
					<li>Log in to your dashboard</li>
					<li>Complete your profile (add a profile picture)</li>
					<li>Explore available features</li>
				</ul>
			</div>
			<p>If this wasn’t you, please contact our support team immediately.</p>
			<p style="font-size:13px; color:#555;">Need help? Just reply to this email.</p>
		</div>
		<div class="footer">
			&copy; <%= new Date().getFullYear() %> YourApp. All rights reserved.<br>
			You are receiving this because you created an account at YourApp.
		</div>
	</div>
</body>
</html>
